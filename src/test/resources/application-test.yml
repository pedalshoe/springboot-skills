## YAML Template.
---
spring:
    application:
        name: springboot-skills-test

    datasource:
        url: jdbc:h2:mem:appdb
        driverClassName: org.h2.Driver
        username: sa
        password:
    sql.init.mode: always

    jpa:
        hibernate:
            ddl-auto: create-drop   # we already created the table; don’t let Hibernate change it
        properties:
            hibernate:
                jdbc:
                    time_zone: UTC
    open-in-view: false
